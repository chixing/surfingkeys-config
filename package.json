{
  "name": "surfingkeys-config",
  "version": "1.0.0",
  "description": "TypeScript-based SurfingKeys configuration",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "type-check": "tsc --noEmit",
    "deploy": "npm run build && node update-gist.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3"
  }
}
